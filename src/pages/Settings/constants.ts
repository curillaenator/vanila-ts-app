import { LIGHT_ICON, DARK_ICON } from '@src/shared/icons';
import type { ColorMode } from '@src/types';

export const COLOR_MODE_ICONS_ASSOC: Record<ColorMode, string> = {
  light: LIGHT_ICON,
  dark: DARK_ICON,
};
