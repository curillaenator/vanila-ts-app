import { TASKS_ICON, GRID_ICON } from '@src/shared/icons';

export const DEFAULT_ROUTE = 'tasks';

export const ICONS_ASSOC: Record<string, string> = {
  tasks: TASKS_ICON,
  launchtabs: GRID_ICON,
};
